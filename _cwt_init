# cwt 공통 초기화 헬퍼
# 사용법: _cwt_init || return 1
# 설정되는 변수: _cwt_git_root, _cwt_worktrees_dir

_cwt_git_root=$(git rev-parse --show-toplevel 2>/dev/null)
if [[ $? -ne 0 ]]; then
  echo "error: git 저장소가 아닙니다." >&2
  return 1
fi

_cwt_worktrees_dir="${_cwt_git_root}/.claude/worktrees"

# ShellCheck configuration for cwt
# ShellCheck doesn't fully support zsh, so we use bash mode
# which catches most real issues while producing some false positives.

shell=bash

# SC1090: Can't follow non-constant source (dynamic sourcing)
disable=SC1090

# SC1091: Not following sourced file (file not found at analysis time)
disable=SC1091

# SC2034: Variable appears unused (variables used by callers/zsh framework)
disable=SC2034

# SC2154: Variable referenced but not assigned (assigned by callers/zsh)
disable=SC2154
